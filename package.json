{
  "name": "mixdrinks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nuxt",
    "prepare": "nuxt --hostname '0.0.0.0' --port 8080",
    "build": "nuxt build",
    "start": "nuxt start",
    "export": "nuxt export",
    "serve": "nuxt serve",
    "buildstart": "nuxt build && nuxt start"
  },
  "engines": {
    "node": "14.16.0"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.6",
    "@nuxtjs/style-resources": "1.2.1",
    "cross-env": "^7.0.3",
    "node-sass": "^4.13.1",
    "nuxt": "^2.15.8",
    "sass-loader": "^10.1.1",
    "serve": "^13.0.2"
  },
  "devDependencies": {
    "@nuxtjs/device": "^2.1.0"
  }
}
